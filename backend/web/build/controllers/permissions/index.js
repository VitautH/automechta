define(['jquery', 'application', 'gridview'], function ($, application, gridview) {
    'use strict';

    var pjaxGridId = '#permissions_grid_wrapper',
        gridViewHelper = new gridview({'pjaxGridSelector' : pjaxGridId});
});